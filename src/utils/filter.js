// import {FilterType} from "../const";

const filter = {
  // [FilterType.EVERYTHING]: (points) => points.filter((point) => ),
  // [FilterType.FUTURE]: (points) => points.filter((point) => ),
  // [FilterType.past]: (points) => points.filter((point) => )
};
export {
  filter
};
